apiVersion: v1
kind: Secret
metadata:
  name: alertmanager-email-config
  namespace: default
stringData:
  alertmanager.yaml: |
    global:
      smtp_smarthost: 'smtp.gmail.com:587'
      smtp_from: 'khaledelnabawy457@gmail.com'
      smtp_auth_username: 'khaledelnabawy457@gmail.com'
      smtp_auth_password: 'password'
      smtp_require_tls: true

    route:
      receiver: 'email-notifications'

    receivers:
      - name: 'email-notifications'
        email_configs:
          - to: 'khaledelnabawy457@gmail.com'
            send_resolved: true
